<% @products.each do |e|%>

<p>
    製品:<%= e.name %>
    価格:<%= e.price %>
    <%= link_to "削除", product_path(e), data: {"turbo-method": :delete} %>
</p>
<% end %>

<%= link_to "新規追加", new_product_path %>